[ [ ( CommentTok
    , "# file must not start with a continuation line"
    )
  ]
, [ ( ErrorTok , " x" ) ]
, []
, [ ( CommentTok
    , "# unquoted identifier must not start with colon"
    )
  ]
, [ ( ErrorTok , ":" ) , ( NormalTok , "x" ) ]
, []
, [ ( CommentTok
    , "# unquoted identifier must not start with comma"
    )
  ]
, [ ( ErrorTok , "," ) , ( NormalTok , "x" ) ]
]
